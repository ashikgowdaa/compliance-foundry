
import React from 'react'

const PageTitle = ({title}:{title:string}) => {
  return (
    <div className='text-text-blue secondary-font text-md font-medium uppercase text-center'>{title}</div>
  )
}

export default PageTitle